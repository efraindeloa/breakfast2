import React from 'react';
import PaymentMethodsScreen from './PaymentMethodsScreen';

// Inicialmente: mismo contenido que comensal (antes de personalizar)
const StatisticsRestaurantScreen: React.FC = () => {
  return <PaymentMethodsScreen />;
};

export default StatisticsRestaurantScreen;

